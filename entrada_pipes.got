When you play the game of thrones, you win or you die

First_of_his_name()

    ITS_KNOWN a[10], b[10][10], c As Stark;

    intervalo[0:20](x)
        => filter[ x % 2 == 0 ]
        => firstN [5+5]
        => foreach[{
            maester_write(x);
            maester_write("\n");
        }];

and_now_his_watch_is_ended
